# v0.12.1 — skip/reuse after compact, role-block query, cache usage

| 项 | 值 |
|---|---|
| 版本 | **0.12.1**（git tag `v0.12.1`） |
| 日期 | 2026-09-10 |
| 判定 | **GO**：0.8B `server_smoke` T1–T5（`suffix_cont` skip + compact `drop_reuse` + `prompt_cache_*`）；host `kvmem_store_test` / `kvmem_runtime_test` |
| 设备 | 日常 **RTX 5050**（0.8B）；27B / IQ3_S 仅 **RTX 5090** |
| llama.cpp | 与 `v0.12.0` 同一 pin `b81c99b` + `patches/0001`–`0004`（submodule 工作树不进此 tag） |
| 产品默认 | 同 `v0.12.0` |
| 前一版 | [`v0.12.0`](v0.12.0.md)（same-query skip / recency suffix / stream usage） |

叠在 `v0.12.0` skip 上修 live 路径的几个炸点。速度不当失败。不改 FA。不窗口化 pos。不并进上游 `llama-server`。llama.cpp 脏树不进 tag。

---

## 1. 这一版修什么

### 1.1 Compact / 改写历史不能走 skip

`v0.12.0` 的 `same_query` 只认 last-user。dsh `/compact` 换掉中间 tool 历史后，last-user 文本和 token span 都还在，于是 `query_reuse_q` 把十几万新 token 往已 pin 的 gen-reserve 里塞：`no free GPU slot` + `non-consecutive ... after <old tip>`。

续轮只看 **LCP 是否顶到上一轮 cache**：

```text
suffix_cont  ⇔  reused 且  (n_cached - n_past) ≤ last_n_gen + 64
```

64 是模板对不齐。不满足则 `prefix_rewrite drop_reuse=1`，`memory_clear_all`，从 0 retrieval。出错例：`n_cached=184148` `n_past=20133`。

`warm_skip` / `query_reuse_q` 都要求 `suffix_cont`。

### 1.2 不要在 query 上 recapture（M-RoPE）

`n_past > query_end` 时不能 `llama_decode` 在 `q0`（seq_pos_max 仍是 `n_past-1`）。本版：

- `query_reuse_q`：keep 已捕获的 Q，只 `apply_retrieval` + prefill `n_new`
- `begin_cached_turn_keep_query`：续轮不清 Q 累加器
- `gdn_sync_to` 只在 `!past_query` 时跑

### 1.3 Query 定位：role block，不是 `rfind`

`prompt.rfind(last_user)` 会命中 `<think>` 里的引用，query span 跳到 think 里。改为最后一个 ChatML `<|im_start|>user` 块，其 **trimmed 内容等于** last-user。对不上不退回「最后一个 user 头」（Qwen tool 常渲染成 user + `<tool_response>`），退回 query-last 64。`same_query` 比 last-user **文本**，不是 token span。

### 1.4 DeepSeek `usage.prompt_cache_*`

```text
prompt_cache_hit_tokens  = n_past   （复用前缀，不论是否还在 GPU）
prompt_cache_miss_tokens = n_prompt - n_past
```

`prompt_tokens = hit + miss`。不发 OpenAI `prompt_tokens_details.cached_tokens`。

---

## 2. 怎么复现

```bash
git checkout v0.12.1
git submodule update --init
source scripts/gpu.sh small
scripts/apply-patches.sh
scripts/build-cuda.sh
.venv/bin/ctest --test-dir build --output-on-failure -R kvmem
python3 scripts/server_smoke.py --port 18181
```

T5：`suffix_cont=1` `warm_skip=1` `query_loc method=role_block`。compact 样例：`drop_reuse=1`，禁止 `query_reuse_q` / `query_replay_skip_same`，仍召回 BLUEBIRD-42。stream usage 含 `prompt_cache_*`。

---

## 3. 质量

- host `kvmem_store_test` / `kvmem_runtime_test` PASS
- 0.8B Q8_0 / 5050 `server_smoke` PASS
- 27B IQ3：本二进制已在 5090 `:18200` 上跑；compact 冷 prefill 159k 后 decode ~53 tok/s。旧 184k cache 不随 tag 保留

未进此 tag：阶段 D `state_write`；T6 `json_schema`；OpenAI error 对象（失败仍可能是字符串 `prefill/retrieval failed`）；session park；gen-slot recycle；MTP 产品默认仍 **none**。
