# v0.5.0 — KVMem × llama.cpp MTP 方案 B（P7 milestone）

| 项 | 值 |
|---|---|
| 版本 | **0.5.0**（git tag `v0.5.0`） |
| 日期 | 2026-09-05 |
| 判定 | **GO**：0.8B / 27B retrieval+MTP 关思考后召回 BLUEBIRD-42；MTP 池 = `budget+gen_reserve` 不随 `-c`；27B 8k–64k 主 KV 32 MiB + MTP KV 2 MiB 不变。速度只记录 |
| 设备 | 日常 **RTX 5050**（0.8B）；27B 仅 **RTX 5090** |
| llama.cpp | submodule pin `b81c99b479d4c24e5eeca10de99032ebd343ef8f` + `patches/0001`–`0003`（与 `v0.4.0` 同一 pin） |
| 产品默认 | `--kvmem` = retrieval + query-last 64。MTP 可选：`--spec-type draft-mtp --spec-draft-n-max N`（默认 **none**）。ISTA IQ3_S **不开 MTP、不再测** |
| 前一版 | [`v0.4.0`](v0.4.0.md)（P4/P5 hybrid + serving） |
| 计划 | [kvmem-mtp-plan.md](../kvmem-mtp-plan.md) |
| 原始日志 | [v0.5.0-quality.log](v0.5.0-quality.log)、[v0.5.0-speed.log](v0.5.0-speed.log) |

成功标准是「llama.cpp 能跑 KVMem + MTP，且 MTP KV 与主干同一套槽位池」。不搬 qw3 的 `generate_mtp`。速度不当失败。

---

## 1. 这一版是什么

在 `v0.4.0` 的 hybrid retrieval + 独立 server 上，收 **P7-0～P7-3**（方案 B）以及 verify 时 GDN **`n_rs_seq` GPU 平面**（不再每步 150 MiB host dump）。

能用的能力：

- CLI / server 认 `--spec-type draft-mtp --spec-draft-n-max N`（与 `llama-server` 相同 flag）。Prefill 之后 decode 走 `common_speculative`，每个 target ubatch 后 `process()` 让 draft KV 跟上
- 工厂：MTP 且 `ctx_other` 是 KVMem → `llama_memory_kvmem_mtp`；池大小从 target 抄，禁止按 draft `n_ctx` 重算。`is_mem_shared` 还要求两个 context 是同一份 `llama_memory`（Gemma4），否则 Qwen 的 `ctx_other` 只给工厂找 target
- Follower：同一 `block_id` → 同一 slot、同一原始 pos。Retrieval 只打主干 K；MTP 跟 selected 集合写回。Query replay 对 draft 同一 `[p0,p1)` `seq_rm`，靠 `process()` 补后缀
- pin 之后 verify n>1 不捕主干/MTP raw-K（未 accept 的草稿不进 raw）
- Hybrid MTP reject：target `n_rs_seq = n_max`。短后缀 `seq_rm` 走 GDN GPU 平面；query replay 挖洞仍只动 attn
- 27B Unsloth `UD-Q4_K_M` 主 GGUF 已焊 `blk.64.nextn.*`，开 MTP **不必**再下 sidecar `MTP/mtp-*.gguf`

明确还没做：

- P6 Metal / Vulkan
- 128k 显存金丝雀（64k 已过关）
- DFlash2 / `--spec-type draft-dflash`
- host NeoX → `ggml_rope`；NVMe 异步 stage 的原 P3-2 退出标准

---

## 2. 怎么复现这一版

日常 0.8B 必须用 **MTP GGUF**（`unsloth/Qwen3.5-0.8B-GGUF` 没有 nextn）：

```bash
git checkout v0.5.0
git submodule update --init
source scripts/gpu.sh small
scripts/apply-patches.sh
scripts/build-cuda.sh
scripts/download-test-models.sh mtp   # models/unsloth/Qwen3.5-0.8B-MTP-GGUF/

python3 scripts/mtp_canary.py --gpu small
```

27B（仅 5090）：

```bash
source scripts/gpu.sh 27b
python3 scripts/mtp_vram_canary.py --gpu 27b
# needle: --no-think, --spec-type draft-mtp --spec-draft-n-max 2, budget 256
```

---

## 3. 质量

| 测试 | 卡 | 结果 |
|---|---|---|
| identity 无 MTP（0.8B 日常 Q8_0） | 5050 | **PASS** |
| no-kvmem + draft-mtp | 5050 | **PASS** 能生成 |
| kvmem recency + MTP `n_max=2` | 5050 | **PASS**；`spec_ckpt tgt=RS n_rs_seq=2`；`n_restore=0` |
| MTP pool cells=512 ≠ n_ctx=8192 | 5050 | **PASS**（1 MiB） |
| recency 256 + MTP 中间针 | 5050 | **PASS** 不召回 |
| retrieval 256 + MTP `--no-think` | 5050 | **PASS** needle 13；`mtp_follow n_no_raw=0`；**BLUEBIRD-42** |
| 27B recency 256 + MTP | 5090 | **PASS** 「no secret code」；全部 `ckpt_bytes=0` |
| 27B retrieval 256 + MTP `--no-think` | 5090 | **PASS** needle 13；`mtp_pool 2 MiB`；**BLUEBIRD-42** |
| P7-3 8k–64k 固定 budget | 5090 | **PASS** 主 KV 32 MiB + MTP 2 MiB 不变 |

---

## 4. 速度（只记录）

27B / 5090，decode = `KVMEM_GEN_WALL`。`n_rs_seq` 之前 MTP 约 19–22 tok/s（每步 host GDN ~150 MiB）；之后 off ~40–45，**off+MTP ~50–56（约 1.2×）**。

P7-3 长 ctx（budget 256）：8k off 37.8 vs retr+MTP 61.6；64k off 32.2 vs retr+MTP 49.3。

---

## 5. 相对计划的落地差异

| 计划原文 | 本版本实际 |
|---|---|
| 不改 `speculative.cpp` 状态机 | 未改 draft/verify 循环。两处薄接线：MTP `ctx_other` 传给工厂；`is_mem_shared` 要求同一 `llama_memory` |
| P7-0 工厂对 MTP 仍可 nullptr | P7-0 当时如此；本 tag 已是方案 B follower |
| P7-0 host `PARTIAL_ONLY` 每步 snapshot | 被 `n_rs_seq = n_max` 取代；host ckpt 仅 `draft > n_rs_seq` |
| 方案 A 当产品路径 | 不做。27B@64k MTP KV 2 MiB vs 方案 A ~258 MiB |

---

## 6. 已知限制

- 生成不能无限长（`gen_reserve`，与 v0.4.0 相同）
- 日常 `Qwen3.5-0.8B-GGUF` **没有** nextn；P7 测试用 `Qwen3.5-0.8B-MTP-GGUF`
- 27B 关思考：必须 `--no-think`，否则 thinking 吃掉 `-n` 会假 miss
- Server 仍单 slot。P6 未开始
- **ISTA IQ3_S 不要开 MTP。** 2026-09-05 5090 16k 扫（`logs/ista_iq3_16k_speed.log`，`-b 512`，`n_max=3`）：接受率 28–41%；16k off+mtp decode 40.6 vs stock 47.3。GGUF 文件名带 `-mtp` 只表示焊了 nextn，默认仍 `--spec-type none`。以后 IQ3 配方、harvest T5、256k 都不要加 `--spec-type draft-mtp`。MTP 金丝雀继续用 0.8B-MTP-GGUF 和 Unsloth 27B Q4_K_M（见 [architecture.md](../architecture.md#ista-iq3_s-mtp-off-do-not-test)）

---

## 7. 补丁与仓库形态

```
patches/0001  工厂 hook；MTP ctx_other；CMake 编 mtp.cpp；is_mem_shared 同一 memory
patches/0002  graph_mtp pre-RoPE K capture；process_ubatch 按 DECODER_MTP 分边 harvest
patches/0003  未改 FA
src/adapter/llama-memory-kvmem-mtp.{h,cpp}   follower 槽位池
tools/kvmem-spec.{h,cpp}                     draft-mtp 接线
scripts/mtp_canary.py / mtp_vram_canary.py
```

llama.cpp gitlink 仍钉 `b81c99b`。升级 pin：`scripts/apply-patches.sh` 后跑 `mtp_canary.py` 与 `mtp_vram_canary.py`。
